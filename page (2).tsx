"use client";
import Link from "next/link";
import {FormEvent,useState} from "react";

type Mode="login"|"register";

export default function Portal(){
 const[mode,setMode]=useState<Mode>("login");
 const[step,setStep]=useState<"details"|"code"|"portal">("details");
 const[showHelp,setShowHelp]=useState(false);
 const[name,setName]=useState("");
 const submitDetails=(e:FormEvent)=>{e.preventDefault();setStep("code")};
 const submitCode=(e:FormEvent)=>{e.preventDefault();setStep("portal")};
 if(step!=="portal")return <main className="auth-page"><section className="auth-brand"><Link href="/"><img src="/assets/nuventra-logo.png" alt="Nuventra Limited"/></Link><div><span className="eyebrow">Secure customer access</span><h1>Your account.<br/><span className="gradient-text">Safely in your hands.</span></h1><p>View your balance, payment history and Customer Benefits Programme progress in one secure place.</p></div><div className="security-points"><span>✓</span><p><strong>Two-step verification</strong><br/>Your account details are checked before a one-time security code is required.</p></div></section><section className="auth-panel"><Link className="auth-back" href="/">← Back to Nuventra</Link><div className="auth-card"><div className="auth-progress"><span className="active">1</span><i></i><span className={step==="code"?"active":""}>2</span></div>{step==="details"?<><p className="auth-kicker">{mode==="login"?"CUSTOMER LOGIN":"REGISTER FOR ONLINE ACCESS"}</p><h2>{mode==="login"?"Welcome back":"Create your secure access"}</h2><p className="auth-intro">{mode==="login"?"Enter all three details exactly as they appear on your Nuventra correspondence.":"We’ll match these details to your account before verifying your registered contact information."}</p><form className="auth-form" onSubmit={submitDetails}><label>Customer’s full name<input required autoComplete="name" value={name} onChange={e=>setName(e.target.value)} placeholder="Enter your full legal name"/></label><label>Nuventra reference<input required autoComplete="off" placeholder="For example: NUV-123456"/></label><label>Client reference<input required autoComplete="off" placeholder="Shown on your correspondence"/></label>{mode==="register"&&<><label>Email address<input required type="email" autoComplete="email" placeholder="Your registered email address"/></label><label>Mobile telephone number<input required type="tel" autoComplete="tel" placeholder="Your registered mobile number"/></label></>}<button className="btn auth-submit" type="submit">Continue securely →</button></form><p className="auth-switch">{mode==="login"?"First time using the portal? ":"Already registered? "}<button onClick={()=>setMode(mode==="login"?"register":"login")}>{mode==="login"?"Register for online access":"Return to login"}</button></p></>:<><p className="auth-kicker">TWO-STEP VERIFICATION</p><h2>Enter your security code</h2><p className="auth-intro">We’ve sent a six-digit, single-use code to the verified contact details held on the account for <strong>{name||"the customer"}</strong>.</p><form className="auth-form" onSubmit={submitCode}><label>Six-digit security code<input className="code-input" required inputMode="numeric" autoComplete="one-time-code" pattern="[0-9]{6}" maxLength={6} placeholder="000000"/></label><button className="btn auth-submit" type="submit">Verify and open portal →</button></form><button className="link-button" onClick={()=>setStep("details")}>← Check or change your details</button><p className="code-help">Code not received? You can request another code after 60 seconds or contact our customer support team.</p></>}</div><p className="auth-privacy">Your information is encrypted in transit. Never share a security code with anyone, including someone claiming to be from Nuventra.</p></section></main>;
 return <Dashboard name={name||"Alex Pennington"} showHelp={showHelp} setShowHelp={setShowHelp} signOut={()=>setStep("details")}/>;
}

function Dashboard({name,showHelp,setShowHelp,signOut}:{name:string;showHelp:boolean;setShowHelp:(v:boolean)=>void;signOut:()=>void}){return <main className="portal-shell"><aside className="portal-nav"><Link className="brand" href="/"><img src="/assets/nuventra-logo.png" alt="Nuventra Limited"/></Link><nav><a className="active" href="#overview">Overview</a><a href="#payments">Payments</a><a href="#benefits">My benefits</a><a href="#documents">Documents</a></nav><button className="help-link" onClick={()=>setShowHelp(true)}>♡ I need some support</button><button className="signout-link" onClick={signOut}>Sign out securely</button></aside><section className="portal-main" id="overview"><header className="portal-header"><div><p className="eyebrow">Customer portal</p><h1>Good morning, {name.split(" ")[0]}.</h1><p>You’re making steady progress. Here’s where things stand today.</p></div><div className="avatar">{name.split(" ").map(n=>n[0]).slice(0,2).join("").toUpperCase()}</div></header><div className="dashboard-grid"><article className="dashboard-card"><span className="label">CURRENT BALANCE</span><strong>£850.00</strong><p>Starting balance £1,000.00</p><div className="mini-progress"><span style={{width:"15%"}}/></div><small>£150.00 paid so far</small></article><article className="dashboard-card"><span className="label">BENEFITS EARNED</span><strong>£75.00</strong><p>Potential total benefit £150.00</p><div className="mini-progress"><span style={{width:"50%"}}/></div><small>6 of 12 monthly milestones</small></article><article className="dashboard-card next-card"><span className="label">NEXT PAYMENT</span><strong>£25.00</strong><p>Due 14 August 2026</p><button className="btn">Manage payment</button></article></div><div className="portal-row"><article className="dashboard-card milestone" id="benefits"><span className="label">YOUR BENEFITS JOURNEY</span><h2>Halfway to your full benefit</h2><div className="months">{Array.from({length:12},(_,i)=><span className={i<6?"done":""} key={i}>{i<6?"✓":i+1}</span>)}</div><p>Complete your next agreed payment to add another <strong>£12.50</strong> to your benefit.</p></article><article className="dashboard-card flexibility"><span className="label">AVAILABLE FLEXIBILITY</span><h2>Life doesn’t always follow the plan.</h2><div><span>Ⅱ</span><p><strong>Payment break</strong><br/><small>Available to request</small></p></div><div><span>↻</span><p><strong>Plan review</strong><br/><small>Available to request</small></p></div><button className="outline-button" onClick={()=>setShowHelp(true)}>View my options</button></article></div><section className="dashboard-card activity" id="payments"><span className="label">RECENT ACTIVITY</span><h2>Your latest payments</h2><table><tbody><tr><td><span className="paid">✓</span>14 July 2026</td><td>Monthly payment</td><td>£25.00</td><td className="earned">+ £12.50 benefit</td></tr><tr><td><span className="paid">✓</span>14 June 2026</td><td>Monthly payment</td><td>£25.00</td><td className="earned">+ £12.50 benefit</td></tr></tbody></table></section></section>{showHelp&&<div className="modal-backdrop" onClick={()=>setShowHelp(false)}><div className="modal" role="dialog" aria-modal="true" aria-labelledby="help-title" onClick={e=>e.stopPropagation()}><button className="close" aria-label="Close" onClick={()=>setShowHelp(false)}>×</button><p className="eyebrow">We’re here to help</p><h2 id="help-title">What would help right now?</h2><p>Asking for support will not affect how we treat you.</p><button className="support-option">My payment is no longer affordable <span>→</span></button><button className="support-option">My circumstances have changed <span>→</span></button><button className="support-option">I’d like to speak to someone <span>→</span></button></div></div>}</main>}
